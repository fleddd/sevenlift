class Pages {
    private root = '/i'

    HOME = `${this.root}`
    AI = `${this.root}/ai`
    FOLDERS = `${this.root}/folders`
    PROGRAMS = `${this.root}folders/programs`
    DAY = `${this.root}folders/programs/day`



}

export const PAGES = new Pages()

